好的，信息已经很充分了。我根据你的描述，把需求整理成一份**《个人工作台桌面应用需求分析文档》**，并且对你不确定的部分给出了**我的推荐方案**。你可以直接拿这份文档作为开发蓝图，也可以发给开发者参考。

---

# 个人工作台桌面应用需求分析文档

## 1. 产品概述

**产品名称**：Zzz的工作台  
**产品定位**：一款**完全本地优先、轻量、高信息密度的个人工作台桌面应用**，面向 AI 研发者（模型训练、量化、部署）与个人效率管理。  
**目标用户**：仅你本人使用，Windows / macOS 均可（建议优先支持 Windows，因为你未明确，但 AI 训练常用 Windows + Linux 双环境，应用可跨平台）。  
**核心目标**：把分散的待办、笔记、提示词模板、习惯打卡、信息流（RSS / GitHub / 日历 / 邮件）聚合到一个界面，减少切换成本，并通过统计周报帮助你回顾。

---

## 2. 用户画像与典型使用场景

### 用户画像
- 职业：AI 研发工程师，涉及模型训练、量化、部署全流程。
- 日常工具：GitHub、终端、Python、各类 AI 框架、邮件、日历、RSS。
- 痛点：提示词模板散落在各文档/聊天记录中，每次重复编写；个人任务、打卡、信息分散；缺少一个“今日总览”的面板。

### 典型一天
1. **早上打开应用**：今日面板显示今日待办、日历日程、AI 热点摘要、未读邮件/GitHub 通知数量、今日习惯打卡状态。  
2. **开始工作前**：打开提示词模板库，找到“模型微调”分类下的模板，填充变量后一键复制，粘贴到训练脚本中。  
3. **工作中**：用番茄钟专注 25 分钟，完成后记录一段笔记（如实验参数、报错截图路径）。  
4. **午休后**：查看 RSS 更新的 AI 论文/博客，收藏感兴趣的内容到笔记。  
5. **下班前**：检查今日待办完成情况，手动触发“生成周报”，系统汇总本周任务完成、番茄时长、习惯打卡，生成 Markdown 周报并发送到个人邮箱。  

---

## 3. 功能需求详细说明

### 3.1 今日面板（Dashboard）
- **优先级**：P0（核心）
- **内容**：
  - 今日待办（按优先级排序，可快速勾选）
  - 今日日程（来自日历集成）
  - 习惯打卡卡片（喝水、运动、阅读等）
  - AI 热点摘要（来自 RSS，预置 arXiv、HuggingFace、GitHub Trending 等源）
  - 消息摘要：未读邮件数量、GitHub 通知数量、RSS 未读条目数
- **交互**：点击卡片跳转到对应模块详情；支持自定义面板布局（如拖拽排序）。

### 3.2 任务管理
- **优先级**：P0
- **功能**：
  - **待办清单**：支持添加任务、标记完成、删除、编辑。
  - **任务属性**：优先级（高/中/低）、标签（可自定义）、截止日期、提醒时间、重复规则（每天/每周/每月/自定义）。
  - **视图**：
    - 列表视图（普通待办）
    - 看板视图（按状态分组：待处理/进行中/已完成/已归档）
    - 四象限视图（紧急-重要矩阵，手动拖拽分类）
    - GTD 视图（收件箱、下一步行动、等待、项目、将来/也许）
  - **番茄钟**：内置番茄计时器，可关联任务，记录专注时长。
- **数据关联**：任务可与笔记、项目关联（通过标签或内部 ID）。

### 3.3 笔记与速记
- **优先级**：P0
- **定位**：轻量快速记录，非重型知识库。
- **功能**：
  - 支持 Markdown、富文本、HTML 片段。
  - 快速新建笔记（全局快捷键，如 `Ctrl+N`）。
  - 笔记列表：按更新时间排序，支持标签、搜索（全文搜索可选，后续迭代）。
  - **不做**：双向链接、知识图谱、复杂文档树。
- **推荐**：采用“卡片式”笔记，每条笔记是一张卡片，可以附加标签、所属项目、创建时间。

### 3.4 提示词模板管理（特色核心模块）
- **优先级**：P0（对你最有价值）
- **功能**：
  - **分类管理**：按 AI 工作流阶段分类：数据准备、预训练、微调、量化、部署、推理、调试等（可自定义分类）。
  - **模板内容**：支持纯文本、Markdown、代码块，内容中支持**变量占位符**，例如 `{{model_name}}`、`{{dataset_path}}`、`{{quant_type}}`。
  - **变量填充**：点击“使用模板”，弹出变量填写表单，自动替换后复制到剪贴板。
  - **模板属性**：标签、收藏、使用次数统计、最后使用时间。
  - **搜索**：按标题、内容、标签搜索。
  - **导入/导出**：支持从 JSON/YAML 文件导入模板，也可导出全部模板备份。
- **推荐**：预置一些常用模板（如 LLaMA-Factory 微调指令、AWQ/GPTQ 量化脚本提示、vLLM 部署配置等），后续可自行增删。

### 3.5 习惯打卡与喝水提醒
- **优先级**：P1（重要但可后置）
- **功能**：
  - 自定义习惯（如喝水、运动、阅读、早睡）。
  - 每日打卡面板，连续打卡统计。
  - **喝水提醒**：可设置间隔时间（如每 60 分钟），系统托盘通知弹窗。
  - 打卡数据用于周报统计。

### 3.6 信息聚合（邮件 / 日历 / RSS / GitHub）
- **优先级**：P1（增强模块，可分阶段实现）
- **邮件**：
  - 使用 IMAP 协议收取未读邮件摘要（发件人、主题、时间），不做完整邮件客户端。
  - 支持配置多个邮箱（Gmail、Outlook、QQ 邮箱等）。
  - 点击摘要可打开系统默认邮件客户端或浏览器查看全文。
- **日历**：
  - 使用 CalDAV 协议同步 Google Calendar / Apple Calendar / Outlook。
  - 今日日程显示在今日面板，可查看未来 7 天日程。
- **RSS**：
  - 内置 RSS 订阅源管理，预置 AI 领域源（arXiv cs.AI/cs.LG、HuggingFace Blog、OpenAI Blog、Google AI Blog、GitHub Trending 等）。
  - 今日面板展示热门/更新条目，点击可离线阅读（全文抓取或摘要）。
- **GitHub**：
  - 通过 GitHub API 获取个人通知、参与的 Issue/PR、Star 的项目动态。
  - 显示未读通知数量，点击跳转浏览器。

### 3.7 本地文件管理（轻量）
- **优先级**：P2（可选，你带问号，我建议轻量实现）
- **定位**：不替代系统文件管理器，仅作为“常用文件夹索引”。
- **功能**：
  - 添加常用文件夹（如实验日志、数据集说明、脚本目录），建立索引。
  - 支持文件名搜索、标签、预览（文本/图片/PDF 缩略图）。
  - 可与笔记/任务关联（附上文件路径）。
- **推荐**：如果你觉得不需要，可以砍掉，或仅保留“附件管理”功能，把文件作为笔记附件存在本地。

### 3.8 统计与周报
- **优先级**：P0（核心闭环）
- **功能**：
  - 统计维度：每日完成任务数、任务完成率、番茄钟总时长、习惯打卡次数、各标签/项目耗时。
  - 可视化：周视图柱状图、饼图、热力图（用简单图表库实现）。
  - **周报生成**：自动汇总本周数据，生成 Markdown 格式周报，包含：
    - 本周完成任务列表
    - 各项目任务统计
    - 习惯打卡情况
    - 番茄专注总时长
    - 下周待办（未完成任务）
  - **发送邮箱**：通过 SMTP 发送到指定邮箱（如个人 QQ 邮箱 / 163 邮箱 / Gmail）。支持定时每周日晚发送，也支持手动发送。
  - **推荐**：优先实现手动发送，后续再加定时。

### 3.9 全局快捷键与系统托盘
- **优先级**：P0
- **功能**：
  - 全局快捷键呼出/隐藏主窗口（如 `Alt+Space`）。
  - 系统托盘常驻，显示快捷菜单：打开今日面板、新建任务、新建笔记、开始番茄钟、暂停/恢复提醒。
  - 通知：使用系统原生通知，不依赖应用前台。

---

## 4. 非功能需求

### 4.1 性能与资源占用
- **启动速度**：< 2 秒（冷启动）
- **内存占用**：空闲时 < 150 MB（不含浏览器内核），尽可能低
- **打包体积**：安装包 < 30 MB（非 Electron）
- **响应速度**：列表滚动、切换面板无卡顿，数据量在 1 万条任务/笔记时仍流畅

### 4.2 安全与隐私
- **数据存储**：本地优先，所有数据默认保存在本机。
- **加密**：数据库文件使用 SQLCipher 加密；敏感配置（邮箱密码、API Token）存储在系统钥匙串（Windows Credential Manager / macOS Keychain）。
- **网络传输**：仅当主动使用外部服务（邮件/日历/GitHub/RSS/云同步）时联网，所有请求走 HTTPS。

### 4.3 备份与恢复
- 提供手动备份功能：一键导出全部数据（SQLite 文件 + 附件）为压缩包。
- 自动备份：每周自动备份到指定本地目录；可选上传到阿里云 OSS。
- 恢复：支持从备份压缩包恢复。

### 4.4 数据存储方案（推荐）
- **结构化数据**：本地 SQLite 数据库，保存任务、笔记、模板、打卡、配置等。
- **附件/大文件**：默认保存在本地应用数据目录下的 `attachments` 文件夹；数据库仅存路径。
- **云同步（可选）**：使用阿里云 OSS 作为远程备份/同步端，通过定时任务或手动触发上传/下载。不建议实时同步，避免冲突。
- **数据库加密**：使用 SQLCipher（社区版可免费使用），密钥由用户设置或系统生成。

---

## 5. 技术选型推荐

| 模块          | 推荐技术                                          | 理由                                              |
| ------------- | ------------------------------------------------- | ------------------------------------------------- |
| 桌面框架      | **Tauri 2.0**（Rust 后端 + Web 前端）             | 体积小（~5MB）、内存低、启动快，完美替代 Electron |
| 前端框架      | **React + TypeScript + Vite**                     | 生态成熟、开发效率高                              |
| UI 样式       | **Tailwind CSS + shadcn/ui 或 Radix UI**          | 高密度极简风格，可定制暗色主题                    |
| 状态管理      | **Zustand**                                       | 轻量、简单                                        |
| 数据库        | **SQLite + SQLCipher**                            | 本地嵌入式数据库，加密支持好                      |
| 后端定时任务  | **tokio-cron-scheduler**（Rust）                  | 用于喝水提醒、周报定时发送                        |
| 邮件发送      | **lettre**（Rust）                                | 轻量 SMTP 客户端                                  |
| 邮件收取      | **imap** crate（Rust）                            | 支持 IMAP 协议                                    |
| RSS 解析      | **feed-rs**（Rust）                               | 支持 RSS/Atom                                     |
| CalDAV 客户端 | 使用 HTTP 请求 + `ical` 解析（或 `caldav` crate） | 简单够用                                          |
| GitHub API    | **octocrab**（Rust）                              | 官方推荐                                          |
| 图表          | **Recharts**（前端）                              | 轻量、React 友好                                  |
| 密钥存储      | **keyring** crate（Rust）                         | 跨平台系统钥匙串                                  |
| 全文搜索      | **Tantivy**（Rust，可选）                         | 如果需要快速搜索，后续可集成                      |

**为什么不用 Electron？**  
你明确介意体积、内存、启动速度，Tauri 在这些方面全面优于 Electron，开发体验也足够好，且 Rust 后端适合处理文件、网络、定时任务。

---

## 6. UI/UX 设计建议

### 6.1 整体布局
```
┌──────────────────────────────────────────────┐
│ 侧边栏 │ 主内容区（多面板，可切换）              │
│        │  ┌──────────────────────────────┐   │
│ 今日   │  │ 今日面板 / 任务 / 笔记 / 模板   │   │
│ 任务   │  │  / 打卡 / 统计 / 设置          │   │
│ 笔记   │  │                              │   │
│ 模板   │  │                              │   │
│ 打卡   │  │                              │   │
│ 统计   │  │                              │   │
│ 设置   │  └──────────────────────────────┘   │
└──────────────────────────────────────────────┘
```
- **侧边栏**：图标+文字，可折叠为纯图标；支持自定义顺序。
- **主内容区**：单窗口多面板，通过侧边栏切换；面板内可再分左右栏（如任务列表 + 详情）。
- **顶部**：可放置全局搜索框（后续加入）、快速新建按钮、窗口控制。

### 6.2 视觉风格
- **主题**：默认暗色，深灰背景（#1E1E1E）、卡片高对比（#2A2A2A）、文字浅色（#E5E5E5）。
- **强调色**：可自定义，推荐青色/蓝色系，用于高亮选中项、按钮。
- **密度**：高信息密度，紧凑但留白合理，参考 **Linear** 的简洁、**Raycast** 的列表密度、**Notion** 的暗色质感。
- **动效**：轻量过渡（150ms），不做复杂动画。

### 6.3 特色 UI
- **今日面板**：卡片式网格布局，可拖拽排列。
- **提示词模板**：左侧分类树 + 中间模板列表 + 右侧预览/编辑，支持代码高亮。
- **四象限视图**：2x2 矩阵，任务卡片可拖拽。
- **番茄钟**：顶部小窗或系统托盘气泡，显示倒计时和当前任务。

---

## 7. MVP 范围与迭代计划

### 第一期（MVP，核心可用）
**目标**：解决你最痛的点——任务、笔记、提示词模板、打卡、周报。
- 今日面板（待办 + 打卡 + 提醒）
- 任务管理（列表 + 看板 + 四象限 + 优先级/标签/重复/提醒）
- 笔记（Markdown 快速记录，支持标签）
- 提示词模板管理（分类、变量填充、复制、搜索）
- 习惯打卡 + 喝水提醒（系统托盘通知）
- 统计与周报（手动生成并发送邮箱）
- 本地 SQLite 存储，数据库加密
- 全局快捷键、系统托盘
- 设置页（主题、邮箱配置、备份）

### 第二期（增强聚合）
- 邮件摘要（IMAP）
- 日历集成（CalDAV）
- RSS 订阅与 AI 热点
- GitHub 通知
- 文件索引（轻量）

### 第三期（云同步与高级功能）
- 阿里云 OSS 自动备份/同步
- 全文搜索（Tantivy）
- 数据导出/导入（JSON/Markdown）
- 插件系统（可选）

---

## 8. 项目结构与开发建议

### 8.1 目录结构（建议）
```
workhub/
├── src/                    # 前端
│   ├── components/
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── Tasks.tsx
│   │   ├── Notes.tsx
│   │   ├── Prompts.tsx
│   │   ├── Habits.tsx
│   │   ├── Stats.tsx
│   │   └── Settings.tsx
│   ├── store/              # Zustand stores
│   ├── styles/
│   └── utils/
├── src-tauri/              # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── commands/       # Tauri commands
│   │   ├── db/             # SQLite 操作
│   │   ├── scheduler/      # 定时任务
│   │   ├── mailer/         # 邮件收发
│   │   ├── rss/            # RSS 解析
│   │   ├── caldav/         # 日历同步
│   │   └── github/         # GitHub API
│   ├── Cargo.toml
│   └── tauri.conf.json
├── package.json
└── vite.config.ts
```

### 8.2 开发流程建议
1. **搭建 Tauri + React 项目**，配置 Tailwind、暗色主题。
2. **先实现本地数据层**：设计 SQLite 表结构（任务、笔记、模板、打卡、设置），封装 Rust 命令。
3. **开发核心页面**：今日面板 → 任务 → 笔记 → 模板 → 打卡 → 统计。
4. **加入提醒与番茄钟**：使用后端定时器 + 系统通知。
5. **实现周报生成与邮件发送**。
6. **测试与优化**：启动速度、内存占用、数据加密。
7. **打包发布**：生成 Windows/macOS 安装包。

---

## 9. 风险与注意事项

- **数据库加密**：SQLCipher 需要正确管理密钥，丢失密钥将无法恢复数据。建议在首次启动时让用户设置主密码，或生成随机密钥存入系统钥匙串。
- **邮件/日历协议兼容性**：不同服务商（Gmail、QQ、Outlook）的 IMAP/CalDAV 配置有差异，需要兼容处理；建议先支持 Gmail 和 QQ 邮箱。
- **RSS 全文抓取**：部分 RSS 只提供摘要，抓取全文可能涉及版权或技术限制，MVP 只显示摘要+链接即可。
- **AI 热点预置源**：arXiv 等源更新频繁，注意请求频率，避免被封；可设置每 30 分钟同步一次。
- **Tauri 学习成本**：如果你不熟悉 Rust，初期可能稍慢，但后端逻辑并不复杂，可以逐步掌握。
- **数据备份**：一定要做自动备份，防止 SQLite 损坏或误删。

---

## 10. 总结

根据你的需求，我推荐的技术栈是 **Tauri 2.0 + React + SQLite(SQLCipher)**，整体架构轻量、安全、启动快。核心功能优先级：

**P0：今日面板、任务管理、笔记、提示词模板、习惯打卡、统计周报、本地加密存储**  
**P1：邮件/日历/RSS/GitHub 聚合、喝水提醒**  
**P2：文件管理、云同步、全文搜索**

你可以先按照 MVP 范围开发一个最小可用版本，验证核心流程后再逐步扩展。

---

